# Hooks

Custom React hooks for reusable logic.

Examples:
- useCollections.ts (fetch and manage collections)
- useAuth.ts (authentication state and actions)
- useLocalStorage.ts (localStorage wrapper)
