# API

API integration layer with service functions for backend communication.

Examples:
- collections.api.ts (collection CRUD operations)
- auth.api.ts (authentication operations)
- config.ts (API configuration and base URL)
