# Types

TypeScript type definitions and interfaces.

Examples:
- collection.types.ts (Collection, CollectionItem interfaces)
- api.types.ts (API request/response types)
- user.types.ts (User, AuthUser interfaces)

Note: Share types with backend via ../../../shared/ when possible.
